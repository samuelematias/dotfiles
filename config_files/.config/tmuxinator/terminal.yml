# /Users/samuelematias/.config/tmuxinator/terminal

name: terminal
root: ~/

windows:
  - ping:
      layout: main-horizontal
      panes:
          - ping www.google.com.br
          - fast --upload
